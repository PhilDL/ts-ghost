import { describe, test, expect } from "vitest";

describe("ts-ghost-content-api", () => {
  test("should work", () => {
    expect(1).toBe(1);
  });
});
// const authors = new Endpoint(AuthorSchema);
// const params = { order: "name ASC", limit: 2, page: 3 } as const;
// const result = authors.browse(params);
